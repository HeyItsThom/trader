# Runtime dependencies (pip install -r requirements.txt)
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
certifi>=2024.0.0

# Desktop app only — not needed for Vercel/API deployment
# matplotlib>=3.8.0

# Build-time only — installed automatically by build_mac.sh, not bundled in .app
# pyinstaller>=6.0
# Pillow>=10.0
